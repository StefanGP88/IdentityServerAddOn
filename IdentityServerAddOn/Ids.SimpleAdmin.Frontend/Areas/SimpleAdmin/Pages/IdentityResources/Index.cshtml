@page
@model Ids.SimpleAdmin.Frontend.Areas.SimpleAdmin.Pages.IdentityResources.IndexModel
@{
    Layout = "_NestedIndexLayout";
}

<index-header>
    <div class="col">Name</div>
    <div class="col">Display name</div>
    <div class="col">Description</div>
    <div class="col-2"></div>
</index-header>

@foreach (var item in Model.List.Items)
{
    <index-row identitfier="@item.Id" title="identity resource">
        <div class="col">
            @item.Name
        </div>
        <div class="col">
            @item.DisplayName
        </div>
        <div class="col">
            @item.Description
        </div>
    </index-row>
}

@section Pagenation{
    <partial name="_Pagenation" model="Model.List.ToPagenationInfo()" />
}
